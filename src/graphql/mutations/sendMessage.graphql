mutation sendMessage($input: SendMessageInput!) {
  sendMessage(input: $input) {
    content
    conversationId
    createdAt
    id
    metadata {
      imageUrl
      location {
        lat
        lng
      }
    }
    senderId
    senderUsername
    timestamp
  }
}