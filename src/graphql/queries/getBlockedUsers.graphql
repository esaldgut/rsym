query getBlockedUsers($limit: Int, $nextToken: String) {
  getBlockedUsers(limit: $limit, nextToken: $nextToken) {
    items {
      createdAt
      friend {
        avatar_url
        bio
        email
        name
        sub
        user_type
        username
      }
      friendId
      id
      initiatedBy
      updatedAt
      user {
        avatar_url
        bio
        email
        name
        sub
        user_type
        username
      }
      userId
    }
    nextToken
    total
  }
}