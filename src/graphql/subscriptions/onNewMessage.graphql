subscription onNewMessage($conversationId: ID!) {
  onNewMessage(conversationId: $conversationId) {
    content
    conversationId
    createdAt
    id
    metadata {
      imageUrl
      location {
        lat
        lng
      }
    }
    senderId
    senderUsername
    timestamp
  }
}