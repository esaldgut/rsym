subscription onMessageDelivered($conversationId: ID!) {
  onMessageDelivered(conversationId: $conversationId) {
    content
    conversationId
    createdAt
    id
    metadata {
      imageUrl
      location {
        lat
        lng
      }
    }
    senderId
    senderUsername
    timestamp
  }
}