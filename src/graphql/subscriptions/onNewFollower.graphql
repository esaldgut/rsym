subscription onNewFollower($userId: ID!) {
  onNewFollower(userId: $userId) {
    createdAt
    follower {
      avatar_url
      bio
      email
      name
      sub
      user_type
      username
    }
    followerId
    following {
      avatar_url
      bio
      email
      name
      sub
      user_type
      username
    }
    followingId
    id
    notificationEnabled
  }
}