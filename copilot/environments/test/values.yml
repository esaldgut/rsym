# Valores de configuración para ambiente de test
NEXT_PUBLIC_AWS_REGION: us-east-1
NEXT_PUBLIC_USER_POOL_ID: ${COPILOT_APPLICATION_NAME}.auth.userPoolId
NEXT_PUBLIC_USER_POOL_CLIENT_ID: ${COPILOT_APPLICATION_NAME}.auth.userPoolClientId
NEXT_PUBLIC_IDENTITY_POOL_ID: ${COPILOT_APPLICATION_NAME}.auth.identityPoolId
NEXT_PUBLIC_BUCKET_NAME: amplify-${COPILOT_APPLICATION_NAME}-${COPILOT_ENVIRONMENT_NAME}-bucket
NEXT_PUBLIC_GRAPHQL_ENDPOINT: https://api.${COPILOT_APPLICATION_NAME}.${COPILOT_ENVIRONMENT_NAME}.amplifyapp.com/graphql